# ===== クリップボード (OSC 52) =====
# tmux 内でコピーした内容を OSC 52 経由でローカル端末(WezTerm)の
# クリップボードまで伝播させる。SSH 先(Ubuntu)で動かす想定。
set -g set-clipboard on

# copy-mode (vi) で y を押したらシステムクリップボードにもコピー
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "tmux load-buffer -w -"
